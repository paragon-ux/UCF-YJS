# Build Docs

This directory preserves one-shot kickoff, planning, readiness, and technical
requirements documents used to build the local MVP. Treat these files as
historical implementation context, not the current user-facing reference.

Current public documentation starts at the root `README.md` and `docs/`.
