# M0 C1 Review

Review target: UCF-Yjs M0 C1 authority and protocol decision docs.

Feature objective:

- Create `docs/TRD.md`.
- Create `docs/authority-planes.md`.
- Create `docs/protocol.md`.
- Create `docs/domain-contract.md`.
- Create `docs/provider-contract.md`.

Relevant invariants:

- Yjs convergence is not semantic acceptance.
- Raw Yjs updates are not the normal public agent contract.
- Provider persistence is not domain authority.
- Commands and outcomes are not solely authoritative inside `Y.Doc`.
- Projections are rebuildable and not mutable authority.
- UCF-Yjs must not depend on UCF-RS code, runtime, storage schemas, transaction
  formats, JSONL layouts, or canonical hashes.
- No M1 package scaffold, GUI, Velt, MCP, Git, or W3C work is allowed in C1.

Verification supplied:

- `git diff --check`: pass.
- Invariant grep over C1 docs: pass.

Findings:

- No findings.

Accepted review result:

- C1 is complete.
- Remaining work is C2 canonicalization/checkpoints, C3 offline/security/schema
  decisions, and C4 spike reports.
