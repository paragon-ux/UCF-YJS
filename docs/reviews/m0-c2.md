# M0 C2 Review

Review target: UCF-Yjs M0 C2 canonicalization and checkpoint decision docs.

Feature objective:

- Create `docs/canonicalization.md`.
- Create `docs/checkpoint.md`.

Relevant invariants:

- Checkpoint identity is actor-neutral.
- Capability-filtered agent views are excluded from checkpoint identity.
- Provider snapshots are not accepted checkpoints.
- Checkpoint restore is forward-only.
- Live version excludes awareness, provider IDs, connection state, and display
  preferences.

Verification supplied:

- `git diff --check`: pass.
- Invariant grep over C2 docs: pass.

Findings:

- No findings.

Accepted review result:

- C2 is complete.
- Remaining M0 work is C3 offline/security/schema decisions and C4 spike
  reports.
